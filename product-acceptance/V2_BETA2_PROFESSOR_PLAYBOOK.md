# V2 Beta2 product acceptance — Professor playbook

This playbook explains visible meaning only. It does not grant a verdict, release authority, provider capability, or deployment approval.

The product-owned technical contract is `old-mike-v2-beta2/product-acceptance/4`. Its material hash gate defines `contentByteLength` as the UTF-8 byte length of the exact original JavaScript string and `contentHash` as SHA-256 over the UTF-8 bytes of `JSON.stringify(originalContent)`. No trim, Unicode normalization, case folding, alias, or raw-content SHA fallback is allowed. The contract freezes literal ASCII, CRLF, surrounding-whitespace, combining-mark, non-BMP emoji, and quote/backslash known answers. It also distinguishes the browser DOM LF value from the original API/file CRLF value; the browser journey does not claim pre-DOM CRLF preservation.

The technical run is content-addressed and single-flight for one exact product tree, acceptance bundle, and resolved workspace authority root. Its immutable claim precedes ports, database work, and child creation. STARTED binds the ordered nonsecret environment and executable authorities; the browser publishes a hash-chained observation receipt; the runner cross-checks that receipt against disposable-database aggregates; and PASS requires exact process, listener, and owned-root cleanup. This machinery establishes local technical evidence only. It does not replace the Professor's scholarly judgment or authorize preflight, UAT, release, deployment, migration, or any live provider.

## Preconditions

1. A separate environment preflight identifies one frozen product hash and confirms the product-owned acceptance contract.
2. Use one authorized local researcher account and one authorized project.
3. Confirm that the environment is the local deterministic fixture with disposable PostgreSQL. Do not enter real research data or credentials intended for a live provider.
4. Confirm that the typed launcher terminal, inner outcome, verifier result, contract, vectors, and frozen manifest all report the same `/3` contract identity and acceptance-bundle, contract-file, and vectors-file hashes before reviewing visible meaning.

## Manual review

1. Sign in and open the authorized project.
2. Choose partial material. For every material, inspect its kind, title, and complete original content before generation.
3. Generate once. Confirm three materially distinct directions, one clearly recommended balanced direction, and complete 13-field S0 content.
4. Inspect all three Field Assist strategies for representative fields. Preview, apply, undo, and save only after the visible text is satisfactory.
5. Reload and sign in again. Confirm the same material authority, selected direction, confirmed S0/Assist state, human-readable artifact, revision, and durable status.
6. If completion is unknown, confirm that the interface offers receipt/status reconciliation only and never silently resubmits.
7. Review keyboard focus, live announcements, mobile overflow, language quality, evidence boundaries, and the whole-artifact Human Gate.

The hash gate and total terminal are local technical evidence. The Professor does not author the hash oracle, launcher, or automation driver. A missing terminal after an immutable STARTED record is externally blocked evidence, not a fabricated completion claim. Automated checks support review but cannot decide scholarly coherence, professional quality, or Professor acceptance.
